Use the agent-memory skill.

Delete the `preferences.md` record for user `demo-user`. Read the record first to get the current version token. Use `python3 -m app.memory delete` with that token as `--if-version`. Do not edit files under `memory/` directly. Do not change `.bob/`, `AGENTS.md`, or application code. Finish with `python3 -m app.memory list`.
