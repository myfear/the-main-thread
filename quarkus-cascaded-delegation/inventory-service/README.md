# Inventory service

The middle service for the cascaded delegation demo. It accepts `aud=service-b`, exchanges the token for `aud=service-c`, and calls the audit service.

Run and test the complete system from the [demo README](../README.md).
