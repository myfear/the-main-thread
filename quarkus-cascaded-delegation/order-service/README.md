# Order service

The edge service for the cascaded delegation demo. It accepts `aud=service-a`, exchanges the token for `aud=service-b`, and calls the inventory service.

Run and test the complete system from the [demo README](../README.md).
