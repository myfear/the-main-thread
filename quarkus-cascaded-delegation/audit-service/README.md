# Audit service

The final service for the cascaded delegation demo. It accepts only `aud=service-c` and records the final claim snapshot.

Run and test the complete system from the [demo README](../README.md).
